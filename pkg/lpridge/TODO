1)  We *should* have a reasonable default bandwidth !

2)  should return a simple S3 class with print(), residuals(), fitted(),
   etc.  Possibly use seqXtend() as now in  lokerns() in ../lokern/R/lokerns.R

